// Brand-specific tokens for ServiceCare Jobline.
// Kept separate from shared layout tokens so per-site theming (Phase 4.1) is a
// find-and-replace of this one file, not a hunt through components.
export const brand = {
  name: "ServiceCare Jobline",
  colors: {
    navy: "#0A1930",
    navyLight: "#0F2340",
    gold: "#F0A428",
    goldLight: "#F5B94F",
    teal: "#14B8A6",
  },
  // Import the actual asset in components: import logo from "src/assets/logo.png"
};
