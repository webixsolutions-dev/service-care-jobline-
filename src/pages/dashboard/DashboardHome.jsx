import ComingSoon from "../ComingSoon";

// Job seeker dashboard shell (Audit R1: lives at /dashboard, not /seeker/dashboard).
// Real content + Supabase-backed hooks land in Phase 4.1.
export default function DashboardHome() {
  return <ComingSoon title="Job Seeker Dashboard" />;
}
