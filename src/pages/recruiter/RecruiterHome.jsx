import ComingSoon from "../ComingSoon";

// Recruiter dashboard shell (Audit R1: lives at /recruiter, not /recruiter/dashboard).
// Real content + Supabase-backed hooks land in Phase 4.1.
export default function RecruiterHome() {
  return <ComingSoon title="Recruiter Dashboard" />;
}
